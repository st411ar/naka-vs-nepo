async function init(){const d=await fetch("data.json").then(r=>r.json());const app=document.getElementById("app");for(const t of d.tournaments){const el=document.createElement("details");el.innerHTML=`<summary>${t.name} [${t.tier}]</summary><pre>${JSON.stringify(t.results,null,2)}</pre>`;app.appendChild(el)}}init();