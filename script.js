async function init(){const app=document.getElementById("app");const data=await fetch("data.json").then(r=>r.json());app.innerHTML="";for(const tier of data.tiers){const sec=document.createElement("details");sec.innerHTML=`<summary>${tier.name}</summary>`;app.appendChild(sec);} } init();